import { createSlice } from "@reduxjs/toolkit";


const initialSTate = {};

const foodSlice = createSlice({
    name : 'food',
    initialSTate,
    reducers : {}
})

export const {} = foodSlice.actions ;
export default foodSlice.reducers ; 